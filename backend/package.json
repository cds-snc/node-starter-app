{
  "name": "backend",
  "version": "1.0.0",
  "description": "node+express server and postgresql database",
  "main": "src/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node src/server.js",
    "pg:start": "docker run -d -p 5432:5432 --name sab-db -e POSTGRES_PASSWORD=password postgres"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "pg": "^7.12.1"
  }
}
