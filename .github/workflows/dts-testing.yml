
name: dts-testing

on:
  push:
    branches:
      - master

jobs:  
  testing:
    name: testing
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: run docker compose
      run: docker-compose up
